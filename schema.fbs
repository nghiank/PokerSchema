//Schema for Netty Chat Server
namespace schema;

union Data {CredentialToken, ReconnectKey}

table ReconnectKey {
  key:string (required);
}

table CredentialToken {
  token:string (required);
}

table Message {
  data:Data;
}

root_type Message;
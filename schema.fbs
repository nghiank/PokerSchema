//Schema for Netty Chat Server
namespace schema;

union Data {CredentialToken, ReconnectKey, JoinRoomCommand, QuitCommand}

table JoinRoomCommand {
  
}
table ReconnectKey {
  key:string (required);
}

table CredentialToken {
  token:string (required);
}

table Message {
  data:Data;
}

root_type Message;
